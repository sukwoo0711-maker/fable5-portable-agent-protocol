interface:
  display_name: "Portable Coding Agent Protocol"
  short_description: "Agent workflow with evidence gates."
  default_prompt: "Use $portable-coding-agent-protocol to handle this coding task with task state, scoped edits, self-review, and explicit verification."

policy:
  allow_implicit_invocation: true
